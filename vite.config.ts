import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  // Stripe Checkout return URLs are validated against the APP_URL edge secret,
  // so the dev port must not drift to 5174 when 5173 is busy — fail instead.
  server: {
    port: 5173,
    strictPort: true,
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
})
